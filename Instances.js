import React from 'react';

class Instances extends React.Component {

  static manifest = Object.freeze({
  });

  render() {
    return (
      <div>Under construction: ui-instances</div>
    );
  }
}

export default Instances;
